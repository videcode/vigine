#include "rate.h"
/*
Rate::Rate(){
    //ctor

    this->vertexArr.push_back(glm::vec3(-0.5f, -0.5f, 0.0f)); // left     bottom
    this->vertexArr.push_back(glm::vec3( 0.5f, -0.5f, 0.0f)); // right    bottom
    this->vertexArr.push_back(glm::vec3( 0.5f,  0.5f, 0.0f)); // right    top
    this->vertexArr.push_back(glm::vec3(-0.5f,  0.5f, 0.0f)); // left     top

    this->normalArr.push_back(glm::vec3(0.0f, 0.0f, 1.0f));    // left     bottom
    this->normalArr.push_back(glm::vec3(0.0f, 0.0f, 1.0f));    // right    bottom
    this->normalArr.push_back(glm::vec3(0.0f, 0.0f, 1.0f));    // right    top
    this->normalArr.push_back(glm::vec3(0.0f, 0.0f, 1.0f));    // left     top

    this->colorArr.push_back(glm::vec3(0.9f, 0.0f, 0.2f));     // left     bottom
    this->colorArr.push_back(glm::vec3(0.0f, 0.2f, 0.8f));     // right    bottom
    this->colorArr.push_back(glm::vec3(0.0f, 0.2f, 0.8f));     // right    top
    this->colorArr.push_back(glm::vec3(0.9f, 0.0f, 0.2f));     // left     top

    this->vertexIndexArr = {0, 1, 3, 0, 1, 2};

}

Rate::~Rate(){
    //dtor
}
*/
